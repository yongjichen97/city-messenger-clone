// src/polyfills.ts

/** Evergreen browsers require these. **/
import 'zone.js';  // Included with Angular CLI.

/** Add global for Node.js core modules polyfills **/
(window as any).global = window;
